# Report System - Visual Architecture & Flow Diagrams

## 1. System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        REPORT SYSTEM ARCHITECTURE                        │
└─────────────────────────────────────────────────────────────────────────┘

FRONTEND (React)
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                           │
│  Student Dashboard                          Admin Dashboard             │
│  ┌─────────────────┐                       ┌─────────────────────┐      │
│  │ Send Report Btn │                       │ Reports Tab         │      │
│  └────────┬────────┘                       └────────┬────────────┘      │
│           │                                          │                   │
│  ┌────────▼────────┐                       ┌────────▼────────────┐      │
│  │ StudentReport   │                       │ Fetch All Reports   │      │
│  │ Form Modal      │                       │ (Paginated)         │      │
│  │ ├─ Select Type  │                       │ Display List        │      │
│  │ ├─ Title        │                       │ ├─ Student Info     │      │
│  │ ├─ Description  │                       │ ├─ Report Type      │      │
│  │ ├─ Validate     │                       │ ├─ Status Dropdown  │      │
│  │ └─ Submit       │                       │ └─ Update Status    │      │
│  └────────┬────────┘                       └────────┬────────────┘      │
│           │                                          │                   │
└───────────┼──────────────────────────────────────────┼───────────────────┘
            │ POST /api/v1/reports                    │
            │ {report_type, title, description}       │ GET /api/v1/reports
            │                                          │
            │                                PUT /api/v1/reports/{id}/status
            │                                │ {status}
            │                                │
BACKEND (Laravel)
┌───────────┼────────────────────────────────┼─────────────────────────────┐
│           │                                │                             │
│  ┌────────▼────────┐          ┌────────────▼────────┐                   │
│  │ ReportController│          │ ReportController    │                   │
│  │ - store()       │          │ - index()           │ - updateStatus()  │
│  │ Validate input  │          │ Check admin role    │ Check admin role  │
│  │ Check auth      │          │ Get paginated       │ Validate status   │
│  │ Create report   │          │ Order by created_at │ Update report     │
│  └────────┬────────┘          └────────┬────────────┘                   │
│           │                             │                               │
│  ┌────────▼─────────────────────────────▼──────────┐                   │
│  │         Database (MySQL)                        │                   │
│  │  ┌───────────────────────────────────────────┐  │                   │
│  │  │ Reports Table                             │  │                   │
│  │  │ ├─ id (PK)                                │  │                   │
│  │  │ ├─ user_id (FK to users)                  │  │                   │
│  │  │ ├─ report_type                            │  │                   │
│  │  │ ├─ title                                  │  │                   │
│  │  │ ├─ description                            │  │                   │
│  │  │ ├─ status (pending|reviewed|...           │  │                   │
│  │  │ ├─ created_at, updated_at                 │  │                   │
│  │  │ └─ Foreign key constraint on user_id      │  │                   │
│  │  └───────────────────────────────────────────┘  │                   │
│  └─────────────────────────────────────────────────┘                   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## 2. Data Flow Diagram

### Student Report Submission Flow

```
┌──────────────┐
│ Student User │
└──────┬───────┘
       │ 1. Clicks "Send Report"
       │
       ▼
┌─────────────────────────┐
│ StudentReportForm Modal │◄──── Opens
└──────┬──────────────────┘
       │ 2. Fills form
       │    - Report Type (Bug/Suggestion/Feedback/Other)
       │    - Title (max 100 chars)
       │    - Description (max 500 chars)
       │
       ▼
┌─────────────────────────┐
│ Frontend Validation     │
│ - Check fields not empty│
│ - Check char limits     │
└──────┬──────────────────┘
       │ 3. Validation passed?
       │ ├─ No ──► Show error message (inline)
       │ │
       │ ├─ Yes
       │
       ▼
┌─────────────────────────┐
│ Submit POST request     │
│ /api/v1/reports         │
│ + Auth Token (Bearer)   │
│ + Body (report data)    │
└──────┬──────────────────┘
       │ 4. Send to backend
       │
       ▼
┌─────────────────────────┐
│ ReportController        │
│ - store() method        │
│ - Check auth token ✓    │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│ Backend Validation      │
│ - Validate report_type  │
│ - Validate title length │
│ - Validate description  │
└──────┬──────────────────┘
       │ 5. All valid?
       │ ├─ No ──► 422 Unprocessable Entity
       │ │        (Return to frontend)
       │ │
       │ ├─ Yes
       │
       ▼
┌─────────────────────────┐
│ Create Report in DB     │
│ - user_id (from auth)   │
│ - report_type           │
│ - title                 │
│ - description           │
│ - status = "pending"    │
│ - timestamps            │
└──────┬──────────────────┘
       │ 6. Insert into reports table
       │
       ▼
┌─────────────────────────┐
│ Return 201 Created      │
│ + Report object         │
│ + User relationship     │
└──────┬──────────────────┘
       │ 7. Send success response
       │
       ▼
┌─────────────────────────┐
│ Frontend Success State  │
│ - Show CheckCircle icon │
│ - "Report Sent!"        │
│ - "Thank you message"   │
└──────┬──────────────────┘
       │ 8. Wait 2 seconds
       │
       ▼
┌─────────────────────────┐
│ Close Modal             │
│ Report Submitted ✓      │
└─────────────────────────┘
```

### Admin Report Viewing Flow

```
┌──────────────┐
│ Admin User   │
└──────┬───────┘
       │ 1. Log in to Admin Dashboard
       │
       ▼
┌─────────────────────────┐
│ Click Reports Tab       │
│ in Sidebar              │
└──────┬──────────────────┘
       │ 2. Trigger fetch
       │
       ▼
┌─────────────────────────┐
│ GET /api/v1/reports     │
│ + Auth Token (Bearer)   │
└──────┬──────────────────┘
       │ 3. Send to backend
       │
       ▼
┌─────────────────────────┐
│ ReportController        │
│ - index() method        │
│ - Check auth token ✓    │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│ Authorization Check     │
│ - Is user role 'admin'? │
└──────┬──────────────────┘
       │ 4. Check passed?
       │ ├─ No ──► 403 Forbidden
       │ │        (Return to frontend)
       │ │
       │ ├─ Yes
       │
       ▼
┌─────────────────────────┐
│ Query Database          │
│ - Get all reports       │
│ - Include user info     │
│ - Order by created_at   │
│ - Paginate (20/page)    │
└──────┬──────────────────┘
       │ 5. Fetch from DB
       │
       ▼
┌─────────────────────────┐
│ Return 200 OK           │
│ + Paginated array       │
│ + User data with each   │
│ + Pagination meta       │
└──────┬──────────────────┘
       │ 6. Send success
       │
       ▼
┌─────────────────────────┐
│ Frontend Display        │
│ - Map reports to cards  │
│ - Show student info     │
│ - Show status badge     │
│ - Show type badge       │
│ - Add status dropdown   │
└──────┬──────────────────┘
       │ 7. Render cards
       │ Auto-refresh every 5 seconds
       │
       ▼
┌─────────────────────────┐
│ Reports Page Ready      │
│ Admin can manage ✓      │
└─────────────────────────┘
```

### Status Update Flow

```
┌──────────────┐
│ Admin User   │
└──────┬───────┘
       │ 1. See report with
       │    status = "Pending"
       │
       ▼
┌─────────────────────────┐
│ Click Status Dropdown   │
│ for a report            │
└──────┬──────────────────┘
       │ 2. Dropdown opens
       │    Options shown:
       │    - Pending
       │    - Reviewed
       │    - Resolved
       │    - Closed
       │
       ▼
┌─────────────────────────┐
│ Select New Status       │
│ (e.g., "Reviewed")      │
└──────┬──────────────────┘
       │ 3. Selection made
       │
       ▼
┌─────────────────────────┐
│ PUT /api/v1/reports/{id}│
│ /status                 │
│ + Auth Token (Bearer)   │
│ + Body: {status}        │
└──────┬──────────────────┘
       │ 4. Send update
       │
       ▼
┌─────────────────────────┐
│ ReportController        │
│ - updateStatus() method │
│ - Check auth token ✓    │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│ Authorization Check     │
│ - Is user role 'admin'? │
└──────┬──────────────────┘
       │ 5. Check passed?
       │ └─ Yes
       │
       ▼
┌─────────────────────────┐
│ Validate Status         │
│ - Is in: pending,       │
│   reviewed, resolved,   │
│   closed?               │
└──────┬──────────────────┘
       │ 6. Valid?
       │ └─ Yes
       │
       ▼
┌─────────────────────────┐
│ Update Database         │
│ - Find report by ID     │
│ - Update status column  │
│ - Update updated_at     │
└──────┬──────────────────┘
       │ 7. DB updated
       │
       ▼
┌─────────────────────────┐
│ Return 200 OK           │
│ + Updated report object │
└──────┬──────────────────┘
       │ 8. Send response
       │
       ▼
┌─────────────────────────┐
│ Frontend Update State   │
│ - Update report object  │
│ - Re-render card        │
│ - Status badge changes  │
│   from yellow to blue   │
└──────┬──────────────────┘
       │ 9. Immediate update
       │
       ▼
┌─────────────────────────┐
│ Status Updated ✓        │
│ Report now shows        │
│ "Reviewed"              │
└─────────────────────────┘
```

## 3. Component Hierarchy

```
App
├── Router
│   ├── StudentDashboard
│   │   ├── StudentReportForm (Modal - when needed)
│   │   │   ├── Report Type Selector
│   │   │   ├── Title Input
│   │   │   ├── Description Textarea
│   │   │   └── Action Buttons
│   │   └── [Other student components]
│   │
│   └── AdminDashboard
│       ├── Sidebar Navigation
│       │   └── Reports Tab Option
│       └── Main Content Area
│           └── Reports Tab (when selected)
│               ├── Header
│               ├── Report List
│               │   ├── Report Card 1
│               │   │   ├── Student Info
│               │   │   ├── Report Type Badge
│               │   │   ├── Title & Description
│               │   │   ├── Date
│               │   │   └── Status Dropdown
│               │   ├── Report Card 2
│               │   └── [More Reports...]
│               └── Pagination (if needed)
```

## 4. Status Lifecycle Diagram

```
    ┌─────────────┐
    │   Created   │ (New report submitted)
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │  PENDING    │ (Default status when submitted)
    │   Yellow    │ (Waiting for admin review)
    └──────┬──────┘
           │
           ├────────────────────────────────┐
           │                                │
           ▼                                │
    ┌─────────────┐                        │
    │  REVIEWED   │ (Admin has read it)     │
    │    Blue     │                        │
    └──────┬──────┘                        │
           │                                │
           ├─────────────┬──────────────────┘
           │             │
           ▼             ▼
    ┌──────────────┐  ┌──────────────┐
    │  RESOLVED    │  │   CLOSED     │
    │   Green      │  │    Gray      │
    │(Fixed/Done) │  │(Completed)   │
    └──────────────┘  └──────────────┘
```

## 5. Report Type Classification

```
┌─────────────────────────────────────────────────┐
│           REPORT TYPE BADGES                    │
└─────────────────────────────────────────────────┘

🐛 BUG REPORT
├─ Blue Badge
├─ For: System errors, crashes, glitches
├─ Example: "Chat won't load"
└─ Priority: High

💡 SUGGESTION
├─ Indigo Badge
├─ For: Feature requests, improvements
├─ Example: "Add file sharing"
└─ Priority: Medium

📝 GENERAL FEEDBACK
├─ Purple Badge
├─ For: General opinions, user experience
├─ Example: "Great update, but..."
└─ Priority: Low

📌 OTHER
├─ Gray Badge
├─ For: Miscellaneous items
├─ Example: "Anything else"
└─ Priority: Variable
```

## 6. API Response Examples

### Success Response (Submit Report)
```
Status: 201 Created

{
  "id": 1,
  "user_id": 5,
  "report_type": "bug",
  "title": "Chat feature not loading",
  "description": "When I click messages, counselor list doesn't appear",
  "status": "pending",
  "created_at": "2025-11-28T10:30:00.000000Z",
  "updated_at": "2025-11-28T10:30:00.000000Z",
  "user": {
    "id": 5,
    "name": "Juan Dela Cruz",
    "email": "juan@example.com"
  }
}
```

### Success Response (View Reports)
```
Status: 200 OK

{
  "data": [
    {
      "id": 1,
      "user_id": 5,
      "report_type": "bug",
      "title": "Chat feature not loading",
      "description": "...",
      "status": "pending",
      "created_at": "2025-11-28T10:30:00.000000Z",
      "updated_at": "2025-11-28T10:30:00.000000Z",
      "user": {
        "id": 5,
        "name": "Juan Dela Cruz",
        "email": "juan@example.com"
      }
    }
  ],
  "links": {
    "first": "http://app.local/api/v1/reports?page=1",
    "last": "http://app.local/api/v1/reports?page=1",
    "prev": null,
    "next": null
  },
  "meta": {
    "current_page": 1,
    "from": 1,
    "last_page": 1,
    "per_page": 20,
    "to": 1,
    "total": 1
  }
}
```

---

**This visual guide helps understand the complete report system flow from submission to admin management.**
